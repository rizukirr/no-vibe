# NO-VIBE — <project name>

<!--
AI's working notes for THIS project, like AGENTS.md. Free-form structure —
add sections as they become useful, prune when stale. Operational over
descriptive: every line should change the next AI's behavior in this
codebase. Pair with ~/.no-vibe/NO-VIBE.md (style memory).

Suggested sections (use what serves; not a required structure):

## Where we are
What's been built across all sessions. Compile state. Run command.
Curriculum next.

## Mental model state in this codebase
What the user has shown they understand here. What they haven't seen yet
that an upcoming layer will require.

## Project conventions
Decisions agreed on inside this codebase (error type, module layout,
deferred features). Not Rust-the-language conventions — those go in
project README.

## Pickup hint
Where to resume next session. What the user said about timing or scope.

AI updates this file only when its current contents are wrong, stale,
or missing context the next session needs. Most sessions write nothing.
Whole-file rewrites archive the prior version to .no-vibe/memory/.
-->
